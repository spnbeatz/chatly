import { Button } from "@heroui/react";

export const CancelButton = () => {
    return (
        <Button
            slot="close"
            variant="outline"
        >
            Cancel
        </Button>
    )
}